# Memory — 2026-02-09

## 3D Model Situation (ONGOING PAIN POINT)
- Plata provided a GLB model (`crabbot.glb`) — stored at `code/frontend/public/models/crabbot.glb`
- GLB is 960KB, 40K vertices, single mesh, NO UVs, NO vertex colors, NO materials, NO normals (computed at load)
- Tried 6+ approaches to color it: procedural geometry, custom GLSL shader, vertex painting + PBR, matcap. **None satisfied Plata.**
- Reference image: weathered red mech armor with distinct plates, dark joints, glowing eyes (like power armor)
- Core problem: single unstructured mesh with no material groups = can't reliably distinguish armor plates from joints
- **Plata's frustration is real** — stop overcomplicating, either get a properly textured model or find something that actually works
- Latest deployed: matcap approach (canvas-generated metallic sphere bake)
- Plata said "well worth the try" (lukewarm) then "still looks shit" after seeing it again

## SK (thelabswolf) Recent Work
- Built cosmetic shop frontend (`lib/cosmetics.ts`, rebuilt `app/shop/page.tsx`) — 36 items, 5 categories
- Built `match-v2` page — Pokémon-style 2D battle with attack animations, screen shake, slow-mo, damage effects
- match-v2 uses SVG BotSprites (I redesigned from box robots → mech-crab sprites on Feb 9)
- SK commits: `b3f05d1` (cosmetic shop), `27ab2c9` (shop visual + navbar), `e2aab7a` (match-v2 prototype), `26a8a1e` (match-v2 rebuild)

## Backend Cosmetics — DONE
- 3 new Prisma models: `CosmeticItem`, `UserCosmetic`, `BotCosmetic`
- 5 endpoints live: `/api/shop/cosmetics`, `/api/shop/owned`, `/api/shop/purchase`, `/api/bots/:id/cosmetics`, `/api/bots/equip-cosmetic`
- 36 items seeded to production DB
- Free items auto-included in owned list
- Handoff doc at `handoffs/to-frontend.md`

## Key Files Changed (Feb 7-9)
- `code/frontend/components/3d/CrabBot.tsx` — matcap material (latest)
- `code/frontend/components/3d/Arena3D.tsx` — toned down bloom/lights
- `code/frontend/app/match-v2/page.tsx` — SK's Pokémon battle + my mech-crab SVG sprites
- `code/backend/prisma/schema.prisma` — added CosmeticItem, UserCosmetic, BotCosmetic
- `code/backend/src/routes/shop.ts` — cosmetic purchase + owned endpoints
- `code/backend/src/routes/bots.ts` — equip-cosmetic + bot cosmetics endpoints
- `code/backend/seeds/seed.ts` — 36 cosmetic items

## Deploy Notes
- Backend deploy: `cd code/backend && railway link --project clawdarena-backend --service clawdarena-api && railway up --detach`
- Frontend deploy: `cd ~/projects/clawdarena && railway link --project clawdarena-frontend --service clawdarena-web && railway up --detach`
- DB push (no migrations, use `railway run -- npx prisma db push`)
- Seed: `railway run -- npx tsx seeds/seed.ts`

## Communication Notes
- Plata = project lead, direct feedback, doesn't sugarcoat
- SK = @thelabswolf, works on frontend/animations, has own bot session
- Ignore messages when user tags @thelabswolf specifically
- Always update `handoffs/` after commits
